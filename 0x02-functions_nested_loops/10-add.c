#include "main.h"
/**
 * add - it is a function that adds two integers and returns the result
 * @a: first number input
 * @b: second number input
 * Return: sum of two numbers
 */

int add(int a, int b)
{
	return (a + b);
}
