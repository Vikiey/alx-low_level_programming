#include <stdio.h>

/**
 * main - prints a string using printf followed by a new line
 * Return: 0 (Success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
